[package]
name = "facedetector"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
ort = { version = "2.0.0-rc.12", features = ["cuda"] }
image = "0.25.10"  # Image loading
ndarray = "0.17.2"  # Tensor operations
once_cell = "1.19.0"  # Global session
